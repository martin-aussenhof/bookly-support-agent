# Together AI — https://api.together.ai/settings/api-keys
TOGETHER_API_KEY=

# Neon Postgres connection string (pooled). Powers the global cost meter.
# Copy the "Pooled connection" string from the Neon dashboard.
# Optional: without it the meter still works, but only in memory for this process.
DATABASE_URL=
